services:
  - type: web
    name: resume-analyser-fastapi
    env: python
    buildCommand: ""
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11